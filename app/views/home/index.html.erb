<div class="well">
	<h4>Coffee is the nectar that makes life more enjoyable</h4>
	<p>This Q&A site is dedicated to the questions about coffee for coffee lovers around the world.</p>
</div>

<div class="container">
	<% @questions.each do |q| %>
		<div class="media">
		  <div class="media-left">
		    <a href="#">
		      <img class="media-object" src="<%= q.gravatar %>">
		    </a>
		  </div>
		  <div class="media-body">
		    <h4 class="media-heading"><%= q.email %> asked:</h4>
		    <div><%= time_ago_in_words q.created_at %> ago</div>
		    <div><%= q.body %></div>
		    <div><a href="<%= question_path(q) %>" class="btn btn-success btn-xs">View Answers</a></div>
		   </div>
		 </div>		
	<% end %>
</div>